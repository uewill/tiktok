name: stepflow
version: 1.0.0+1
description: StepFlow - 智能跑步步频节拍器
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  sensors_plus: ^6.1.1
  shared_preferences: ^2.3.2
  vibration: ^3.1.3
  flutter_tts: ^4.1.0
  wakelock_plus: ^1.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
